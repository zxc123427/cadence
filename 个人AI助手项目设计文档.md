# 个人 AI 助手项目设计文档

> 目标：一个类 Jarvis / EDITH 的个人助手，具备多模态感知、主动交互和可扩展工具调用能力，独立运行在家中的小型计算机上。**仅供自己使用。**

> **文档维护说明**
> 本文档是活文档，随时可改，改动本身不需要理由——想法变了就改。三条约定让它不至于越改越乱：
> 1. **正文只放不会过期的判断**（架构原则、取舍理由、纪律）。具体型号、价格、版本号一律进附录，并标注查证日期
> 2. **改结论时保留原因**。把"不该爬大众点评"改成"该爬"没问题，但要留一句为什么变了——半年后你会想知道
> 3. **实际做出来的东西是准的，文档是次准的**。两者冲突时以代码和数据库为准，回头改文档

---

## 1. 项目定位

### 1.1 核心差异化

不在这些地方：模型能力（都调同一批 API）、语音对话（消费级产品做得更好）、外形。这些已经商品化。

真正只有个人项目能做的，按价值排序：

| 差异点 | 说明 |
|---|---|
| **主动性** | Jarvis 的本质是它先开口。所有商业助手都是请求-响应，不问不答 |
| **私有上下文** | 你的文件、日历、习惯、设备状态——云端助手拿不到或你不愿意交出去 |
| **执行权** | 能真的动手：改文件、开灯、跑脚本，而不只是说话 |

后两条是第一条的前提：没有上下文无法判断该不该主动，没有执行权主动了也没用。

### 1.2 与智能音箱的本质区别

老音箱链路：`唤醒词 → ASR → NLU(意图分类+槽位) → 技能路由 → 固定API → NLG模板 → TTS`

核心是**封闭的意图集合**，每个意图对应一段人写的技能代码。这决定了它的天花板：

- 不在意图集里就是"我还不明白"
- 不能组合（"查天气，下雨就把闹钟提前半小时"做不到）
- 多轮靠手写状态机，脆弱
- 回答是模板填充，所以永远像客服

本项目中这一整层消失了，LLM 同时承担意图理解、规划和生成。

其他结构性差异：

| | 智能音箱 | 本项目 |
|---|---|---|
| 交互范式 | 被动，必须唤醒 | 事件驱动，可主动 |
| 记忆 | 基本没有 | 记忆层是核心资产 |
| 模态 | 只有音频 | 视觉 + 音频 + 传感器 |
| 语音 | ASR→NLU→TTS 级联 | 端到端 S2S |

### 1.3 为什么个人能做出更好的东西

**不是技术更强，是不需要对一亿人负责。**

音箱是硬件补贴 + 上亿用户，单次交互边际成本必须逼近零，所以它*必须*用小模型、模板和意图分类。个人项目一天几十次调用、几块钱，可以随便用最强模型、每次塞完整上下文、为一句话花三秒思考。

**护城河**：闭源平台永远不会允许的那类能力——调用你自己电脑上的任意代码、读你的私有数据库、用你的登录态访问网站、按你的规则主动干预。这不是技术限制而是责任限制，不会随模型进步消失。

### 1.4 承认对方的优势

- **远场拾音与唤醒**：五米外、电视开着还能准确唤醒不误触发。多年声学工程 + 海量真实数据，这个差距补不上
- **延迟**：边缘节点、就近接入
- **稳定性**：你的系统会崩，而且会在最需要它的时候崩

策略：**把声学当外设买，不要自己造。**

---

## 2. 系统架构

### 2.1 总体结构

```
入口                        路由                模型
─────────────────────────────────────────────────────
唤醒词/麦克风  ──┐                        ┌─ Realtime WS
摄像头状态机  ──┤                        ├─ VLM
定时器        ──┼─→ 事件总线 → 静态路由 ─┼─ 文本模型
CLI / 手机    ──┘        ↕                └─ TTS
                    记忆层 (SQLite)
```

### 2.2 关键原则：路由几乎不需要"智能"

最常见的坑是让一个模型判断该调哪个模型。**请求从哪个入口进来，就决定了走哪条路**——写 if/else 即可。

唯一需要动态判断的地方：realtime 对话中碰到干不了的活，它自己发 `delegate_task()`。这个决策由模型做，你一行都不用写。

### 2.3 三层触发架构（视觉与成本控制的核心）

不能把连续视频喂给 VLM，成本和延迟都会爆掉。必须分三层：

1. **本地廉价常驻层** — Frigate（人形检测）+ MediaPipe Pose（骨架），跑在本地，永不上传，每秒几十帧无所谓
2. **状态机层** — 骨架序列 → 状态。`在床上 / 坐起 / 离床`、`在座 / 离座`、`站立面向镜子`。**只有状态跃迁才算事件**
3. **昂贵调用层** — 状态跃迁时抓 1–3 帧上云给 VLM，一天几十次，成本几毛钱

> ⚠️ **第二层是全项目最大的自研部分，别被这一行字骗了。** 14.1 把音频归为"没有教程、只能磨"的一类，视觉状态机是同一类：抖动、遮挡、多人、夜间光线，每一个都制造误报，而误报会直接变成骚扰（见第 6 节）。两条纪律现在就定下来：
> - **迟滞 + 最小驻留时间** — 状态必须稳定持续 N 秒才算跃迁。没有这条，你会在"在座/离座"之间一分钟抖十几次
> - **先离线回放调参，再上线** — Frigate 的录像就是现成数据集。对着录像反复跑状态机，比对着实时画面调快一个量级

这个"廉价常驻层 gate 昂贵层"的原则同样适用于语音：本地 VAD/唤醒词先扛住，只在需要时开 realtime session。常开直连 realtime API 会烧钱。

### 2.4 快慢分工

分界线不是"简单 vs 复杂"，是**时长和有无对话**：

| 交给 realtime 直接调 | 交给慢任务层跑 |
|---|---|
| 亚秒级 API：天气、POI、日历、开灯 | 十几秒以上：浏览器自动化、爬取、多页分析 |
| 对话中的多步推理 | 无人说话时的后台任务：定时、摄像头事件 |
| | 跨会话的记忆读写 |

慢任务的正确模式是**下单而不是等待**：realtime 调 `delegate_task()` 立刻返回任务 id，说"我去查，好了叫你"，后台异步跑完，结果通过主动播报回来。别让音频 session 挂着等——纯等待时间也要付音频 token。

**realtime 覆盖不到的场景**：没有对话的时候。早上你还没醒，摄像头检测到离床，此时没有 session。这条链路只能由常驻进程发起。

### 2.5 统一内部消息格式

所有入口转成同一结构，所有出口从它出发：

```json
{
  "source": "camera|voice|timer|cli",
  "type": "state_change|utterance|schedule",
  "payload": {},
  "ts": "2026-08-09T07:12:00Z",
  "context_refs": []
}
```

以后加模型就是加一个 adapter，不动核心逻辑。

### 2.6 重要：交互层不是核心

时间线：GPT-Realtime 2025年8月首发 → 2026年5月 2代 → 7月 2.1。**交互层半年换一代，是最容易被替换的组件。** agent 框架同理。

真正的核心资产是三样：

1. **记忆层** — 偏好、历史、日志、什么让你烦
2. **场景定义** — 什么事件触发什么、什么时候该闭嘴
3. **工具脚本** — CLI 脚本

这三样跨模型、跨框架迁移，写一次能用好几年。**硬规则：把它们彻底放在 realtime session 之外**，session 只是临时读者。换供应商时改的应该只有音频管道那一层。

---

## 3. 技术栈

### 3.1 分层清单

**硬件**
- 树莓派 5（8GB）或 Orange Pi 5 Plus — 主机
- USB 会议音箱（Anker PowerConf / Jabra Speak）或 ReSpeaker 阵列 — **必须带硬件 AEC**
- USB / RTSP 摄像头 ×N
- ESP32-S3（可选，分布式终端）

**音频层**
- openWakeWord — 唤醒词，本地
- Silero VAD — 判断说完了
- PipeWire — 音频路由，软件 AEC 兜底
- **Pipecat 或 LiveKit Agents** — 不要自己写 WebSocket 客户端

**视觉层**
- Frigate（Docker）— 人形检测、录像、事件
- MediaPipe Pose — 骨架关键点
- 自写状态机 — 骨架序列 → 语义状态

**编排层**
- Python 常驻进程（systemd）— 拥有硬件、事件总线、路由、SQLite
- pi (TypeScript) — 慢任务工作台，作为子进程被调用
- APScheduler — 定时事件

**数据层**
- SQLite 单文件 + DB Browser for SQLite
- 定时 `cp` 到另一块盘即备份

**工具层**
- 高德开放平台 POI API
- Python CLI 脚本（`poi.py`、`log_meal.py`…）
- ego-lite（跑在 Mac 上，HTTP 暴露）— 登录态浏览任务
- Home Assistant（可选）— 实体设备控制

**运维**
- systemd（除 Frigate 外不用 Docker）
- `events` 表当日志

### 3.2 语言分裂问题

pi 是 TypeScript，但音频视觉生态几乎全是 Python（openWakeWord、MediaPipe、Silero）。

**方案：Python 拥有硬件和常驻逻辑，pi 只当被调用的工作台。** 边界是进程边界，用 CLI 参数或 HTTP 通信。

如果想彻底避免两种语言：agent 循环本身并不复杂，Python 自写几百行完全可行，pi 的主要价值是多提供商抽象，那部分可用 LiteLLM 替代。

> **结论（2026-08）：v1 不引入 TypeScript，全 Python。**
> 上面三段其实已经把答案说完了——pi 的核心价值可被 LiteLLM 替代，agent 循环几百行 Python 能写，而 14.2 说项目死于子系统数量。多一门语言 = 多一个进程边界 + 多一套依赖 + 多一处交界故障，换来的是一个你现在还说不清具体是什么的好处。
> 等真的撞上"Python 这边写不了"的具体问题时再加。那时你会知道自己在买什么。

### 3.3 关于 pi 框架

- **pi-ai** 是统一的多提供商 LLM 抽象（Anthropic / OpenAI / Google / xAI / Groq / OpenRouter 及 OpenAI 兼容端点），带流式、工具调用、token 与成本追踪。多模型路由基本现成
- **无 MCP 支持**，所有工具自己写。扩展模型是事件总线 + 能力注册，扩展就是普通 TS 函数，可订阅事件、注册能力
- 事件总线正是注入视觉事件、实现主动性的插入点

### 3.4 明确不要的东西

向量数据库、Docker Compose 全家桶、消息队列（Redis/RabbitMQ）、Kubernetes、ORM、前端界面、微服务拆分。

单用户单机规模下，这些每一个都会多花一周，且**一个都不解决实际问题**。

---

## 4. 模型选型

### 4.1 只有对话路径需要 realtime

| 路径 | 需要 realtime？ | 说明 |
|---|---|---|
| 开口对话 | ✅ 必须 | 端到端 S2S 无法替代 |
| 主动播报 | ❌ | 单向输出，文本模型 + TTS，成本低几十倍 |
| 摄像头场景判断 | ❌ | 普通 VLM 调用 |
| 推荐/搜索/脚本 | ❌ | 普通文本 API |

**为什么级联做不到拟人**：STT → 文本模型 → TTS 丢掉两样补不回来的东西——① 进来的语气（"行啊"是开心还是不耐烦，模型只看到两个字）② 轮次节奏（首字响应 800ms–1.5s，打断是外挂逻辑）。端到端模型直接听到韵律也直接生成韵律，打断是原生能力。

### 4.2 选型准则

2.6 说交互层半年换一代——**那么具体型号就是本文档里最先过期的部分**。所以正文只留不会变的判断依据，型号对比放进附录 B 并标注查证日期。

| 准则 | 说明 | 权重 |
|---|---|---|
| **出口位置** | 决定 RTT，RTT 决定对话感（见 10.1）。这条上没有补救手段 | 最高 |
| **是否原生 S2S** | 级联（STT→LLM→TTS）在拟人感上不可比，见 4.1 | 高 |
| **工具调用形态** | 能否并行调用、有无 preamble，直接决定长任务时的对话观感 | 中 |
| **协议复杂度** | 二进制自组帧 vs 标准 WebSocket/JSON，差几天工期 | 中 |
| **计费与配额** | 是否按时长计费、有无免费额度，见 12.1 | 中 |

> **Preamble 值得单独拎出来**：模型在调用工具前先说一句"我查一下这个"。没有它，用户会以为掉线而插话，插话又打断正在进行的工具调用，状态就乱了。这是少数几个"有没有"直接决定体验、而不只是锦上添花的特性。

选型时按这张表打分，别按"谁家模型更强"——4.1 已经说明，这条路径上模型智商不是瓶颈。

### 4.3 Realtime API 的输出内容

**关键：文本不是主输出，音频才是。** 拟人感全在音频里，不以结构化数据暴露——拿不到"语气=温和"这样的字段，也拿不到用户情绪标签。需要显式情绪数值得另配模型（Hume EVI 那类）。

实际收到的事件分四类：

| 类别 | 内容 |
|---|---|
| 音频 | `response.output_audio.delta`，base64 编码字节 |
| 两份转录 | 见下 |
| 时序/VAD | `input_audio_buffer.speech_started` / `speech_stopped` |
| 工具与生命周期 | `function_call_arguments.delta/done`、`response.done`、`rate_limits.updated` |

**两份转录性质完全不同**：
- **输出转录**始终提供，模型原生产出，与音频高度吻合
- **输入转录**默认关闭需在 session 配置开启；由**独立模型**产出，不一定等于模型真正"听到"的，还可能滞后几秒

> ⚠️ **对记忆层的直接影响**：别把输入转录当事实来源写进 preferences。你以为记下"不吃香菜"，存进去可能是"不吃湘菜"。

**最容易做错的一件事：truncate**

服务器产生音频比实时快，用户打断时你手上有一堆已收到未播出的音频。必须发 `conversation.item.truncate` 带上实际播到第几毫秒，让服务端对齐客户端播放进度。截断还会删掉服务端那段文字转录，保证上下文里不存在用户没听到的文本。

不做的后果很隐蔽：模型以为自己说过一段你没听见的话，接着往下讲，对话逐渐变得莫名其妙。这是自建客户端最常见的 bug——也是**建议直接用 Pipecat / LiveKit Agents** 的主要原因。

**两个已知粗糙处**：输出转录无法与语音时序对齐（打断时难精确截断文本、做不了逐词字幕）；快速连续打断容易让 API 跑偏。

---

## 5. 记忆层设计

### 5.1 记忆 ≠ 上下文压缩

这两件事目标相反：

- **上下文压缩**：会话内、有损、为了在窗口塞更多东西。产出是"我们刚才聊了餐厅推荐"
- **记忆**：跨会话、精确、持久事实。产出是"不吃香菜"

用压缩摘要当记忆是最常见的失败方式——会存一堆"用户询问了穿搭建议"，而真正的事实（他说那件蓝衬衫太紧了）在压缩中丢掉了。

### 5.2 写入：三种机制

1. **确定性写入（占大部分，最可靠）** — 根本不经过模型。状态跃迁、起床时间、久坐时长、明确的"记一下我吃了牛肉面"，代码直接写库
2. **显式工具调用** — 给模型 `remember(category, key, value)`，system prompt 写明什么值得记（偏好、约束、明确表述的事实）、什么不值得（临时话题、问句）。可控可审计
3. **会话后离线抽取** — session 关闭后把转录丢给便宜模型输出 JSON 候选。**必须离线，不能在对话链路上**。初期可先落成待审核队列

> ⚠️ **补充纪律（2026-08-11，来自第一次真实使用）：凡是经过模型写入的数据，都必须可回显、可更正。**
>
> 第一天就出了事故：用户说"我还吃了梅干菜烧肉"，库里存成了"牛肉面（复热）"——模型把新菜的属性挂到了上一道菜上。4.3 警告过转录层会出这种错（"香菜"存成"湘菜"），抽取层是同一类问题，**区别只是你多久发现一次**。
>
> 所以第 2、3 种机制必须配套两样东西：**写完当场回显**（哪怕只是打一行"已记录：X"），以及**事后能按条件查出来改掉**。只有第 1 种确定性写入可以省掉这套。

### 5.3 读取：两条路

- **always-on 注入** — 稳定偏好、身份、硬约束直接拼进 system prompt。偏好表可能一共 50 条几百 token，**全塞进去比任何检索都准**
- **按需检索** — `recall(query)`、`query_log(table, since, filters)`

> **别上向量库。** 数据量到明年也就几千条，SQL `LIKE` + 时间过滤完全够，而且**可解释**。向量检索会给出莫名其妙的召回，然后你完全无法 debug 为什么它以为你爱吃日料。

**第三条：每轮都会变的东西，缀在对话末尾，不能放 system prompt（2026-08-11 补）。**

模型没有时钟，得每轮告诉它现在几点，否则"昨天下午"它算不出来。直觉是写进 system prompt——和偏好放一起，都是"背景信息"。**但这两样东西的性质完全不同：偏好几天才变一次，时间每轮都变。**

prompt 缓存按**前缀匹配**：从第 0 个 token 起连续相同的那一段才能复用，一个 token 对不上，从那里往后全部作废。system prompt 在 `messages` 最前面，时间戳嵌在里面每轮一变，**后面整段对话历史就全部失配**——代价不是那几十个 token，是当前对话的全长，而且越聊越贵。插一条独立的 system 时间消息到 `messages[1]` 同样没用，变化点还在前面。

所以时间缀在**每轮最新的 user 消息**上（`llm.user_message()`）。实测每轮命中 1152→1408 token、新增只有约 100；放 system 里这个数会每轮归零。

代价是 `messages` 里那条不再是原话，但 `events` 记的是原始字符串、不走 `messages`，**留痕不受影响**。

> 判据比"时间"这一个例子更通用：**要往 prompt 里塞东西时，先问它多久变一次。** 稳定的往前放，每轮变的往后放。以后加"当前位置""今天日程"也照这条。

### 5.4 Schema：不要覆盖

记忆会过时和矛盾。"我最近在减脂"三个月后还成立吗？

```sql
CREATE TABLE memories (
  id INTEGER PRIMARY KEY,
  category TEXT,        -- preference / constraint / fact
  key TEXT,             -- "香菜"
  value TEXT,
  source TEXT,          -- voice / extraction / manual
  created_at TIMESTAMP,
  expires_at TIMESTAMP, -- 可空，短期状态用
  superseded_by INTEGER -- 指向新版本
);
```

同 key 写入时**追加新版本而非覆盖**，读取取最新未失效的那条。当它说出莫名其妙的话，你能查出：哪条记忆、哪次会话、谁写的。

两个实现细节，现在不注意以后会咬人：

- **`superseded_by` 和 `created_at`/`expires_at` 功能重叠**，而重叠的字段迟早会互相矛盾（改了一个忘了另一个）。起步阶段只留时间戳这一套：按 `key` 取 `created_at` 最新且未过期的一条。等真需要"指明被哪条取代"时再加字段
- **`key` 别直接用自然语言。** 存了"香菜"，那"芫荽""香菜叶"算不算命中？用归一化 key（如 `food.dislike.cilantro`），人话放在 `value` 里

**对个人助手来说，可调试性比智能度重要得多。** 偶尔笨但能查明原因的助手能用两年；聪明但记忆来源不明的助手，第一次莫名其妙之后你就不信它了。

### 5.5 起步四张表

| 表 | 用途 |
|---|---|
| `events` | 所有事件流水，出问题时唯一能查的东西 |
| `memories` | 偏好与约束 |
| `logs` | 饮食、久坐、起床时间 |
| `conversations` | 跨会话对话摘要 |

第五张表 `nudges`（主动发言的提案与结果）见 6.2——它逻辑上属于主动性策略层，但**存在同一个库里**，因为闸门需要读 `logs` 和 `memories` 才能判断该不该开口。

**所有路径读写同一个库。** 否则你会得到四个互不认识的机器人：语音那个不知道摄像头看到了什么，早上播报的那个不知道你昨晚说过今天要早起。

### 5.6 数据库知识门槛

需要的：`CREATE TABLE` + 几个类型、`INSERT`、`SELECT ... WHERE ... ORDER BY ... LIMIT`、加索引。一个下午。

不需要的：范式化理论、JOIN 优化、事务隔离级别、连接池、ORM、迁移框架。

装 **DB Browser for SQLite**——"能亲眼看见表里的数据"比任何教程都有用。

**真正的门槛在 schema 设计**，不在 SQL 语法。三条现在定下来能省很多事：

1. 追加不覆盖
2. **时间戳一律 UTC + ISO 8601**。错了以后所有时间查询都错，且要等到夏令时或出差才暴露
3. 别过早拆表。宽表 + 一个 JSON 字段放杂项，跑半年再重构

**第 2 条的推论：UTC 不出数据层，模型只说本地时间（2026-08-11 补）。**

让模型自己填 UTC，它得把"昨天下午三点"减一天再减八小时。**这种算术它会错，而且错了不报错**——`ts` 是 TEXT 列，一个格式不对或算错的字符串塞进去，SQL 比较不会抛异常，只会安静地返回错误结果。这类 bug 最难查：没有报错、没有堆栈，只有"怎么查不到那条记录"。

所以给模型的工具参数一律收本地时间（`2026-08-10 15:00`），换算收进 `db.from_local()` 一处实现，格式不对当场抛 `ValueError`、绝不猜。`db.to_local()` 是它的逆，给人看时用。**换算逻辑只该有一份**——这次改之前，`log_meal.py` 自己又写了一遍时区转换，格式字面量散在四个地方。

> 更一般的说法：**存储格式和交互格式是两回事，别让上层去迁就存储。** 库里是 UTC ISO 因为它无歧义；人和模型说本地时间因为那才是他们的思考方式。中间那层翻译写一次就够。

### 5.7 安全：收窄接口

**别给模型 `execute_sql(sql)`。** 给参数化固定函数：

```python
recall(category, key)          # 而不是让它自己写 SELECT
log_meal(name, time, note)     # 而不是让它自己写 INSERT
```

原因不是怕攻击，是它会写出意料之外的查询，然后你不知道数据是怎么变成那样的。**收窄接口就是收窄 debug 范围。**

**再收一层：不可逆的权限先别给（2026-08-11 补）。**

窄接口不是只有"给不给 SQL"这一档。同样是窄接口，`correct_log(id, ...)` 和 `delete_log(id)` 的风险差一个量级——**更正可逆，删除不可逆**。

实践下来的分法：

| 操作 | 给模型 | 理由 |
|---|---|---|
| 查 | ✅ | 无风险 |
| 写、改 | ✅ | 可逆。改错了再改一次 |
| 删 | ❌ 留在 CLI | 不可逆。而且真实场景里几乎总是想"改"而非"删" |

也别把它们合并成 `edit_log(..., delete=True)`——一个布尔开关把两种风险等级混进同一个工具，模型传错一个 `True`，后果完全不同。

**判据：这个权限用错一次，你能不能自己救回来？** 不能就先不给，等真的缺了再说。

**还有一条：需要用户点头的操作，确认逻辑必须是确定性代码，而且只能有一个实现点。**

写在 system prompt 里的"删除前请先确认"不算数——模型可能听可能不听，而且事后你查不出这次为什么没听（同 6.1）。确认必须在代码里挡住。

之所以强调"只能有一个实现点"，是因为**这层东西马上就要换**：CLI 阶段它是 `input()` 的 y/N，上语音之后没有 stdin，得改成"登记待确认项 → 模型开口问 → 用户下一轮说'对'"的形式，并且要用代码强制待确认项早于用户最后一次发言（否则模型能同一轮里自问自答）。散在各处写 `input()`，到时候就得满项目找。同 2.6。

> 语音阶段还有一层：那时确认本身是概率性的（4.3 说输入转录由独立模型产出、可能滞后、不一定等于模型真正听到的），**不该把不可逆操作绑在会出错的转录上**。届时可能需要把真删改成软删除。

**待解决：确认该不该分档？（2026-08-11 提出，先记着不做）**

给 `log_meal` 加了 `ts` 之后跑了一次真实更正，全过程正确但**不像人话**：

```
我 > 昨天晚上吃的，说错了
它 > 找到这条：08-10 15:00 草莓冰淇淋（id=6）。你的意思是这条其实是昨天晚上吃的？
     我帮你把时间改成昨晚，可以吧？确认一下你昨晚大概几点吃的。
我 > 昨天晚上7点
它 > [代码弹确认框] 要把这条时间改成「08-10 19:00」吗？ [y/N]
```

改一顿饭的时间点，走了三道关：模型复述、模型追问、代码确认。**现实里只有考试报名、订机票、约人这种事才值得这么问一遍。**这是"确认必须是确定性代码"这条纪律的副作用——它保证了不会漏问，但也保证了每次都问。

两条候选路线，都还没想清楚：

- **代码判别**：按风险分档，比如只改 `note` 不问、改 `ts`/`name` 要问、删除永远问。确定性、可审计，符合本节其余部分。但"风险"这个判据到底怎么写成 if 条件，目前只想得出"按字段列白名单"，很粗
- **交给模型判断**：让它自己决定这次要不要确认。灵活，但直接违反本节开头那条——模型可能听可能不听，事后查不出为什么这次没问

倾向前者：判别逻辑放代码里，模型只管说话。但真要动之前先攒够样本——**记满 20 次真实更正再回来看，哪些当时确实需要问、哪些纯属打断。**现在样本量是 1，不够。

---

## 6. 主动性策略层

> 第 1 节把主动性列为最高价值的差异点，14.3 又指出"它烦不烦人决定它活不活下来"。既然这是生死变量，它就必须有自己的设计，而不是散落在各个场景里。

### 6.1 这一层解决的不是"说什么"，是"该不该说"

产生一句话很容易——事件到了，丢给文本模型，TTS 播出去。难的是这四个判断：

1. **现在该说吗**（我在睡觉 / 在开会 / 五分钟前刚说过）
2. **还值得说吗**（起床播报延迟到九点就没意义了）
3. **今天说得够多了吗**（第六条提醒会毁掉前五条的好感）
4. **上次说完你什么反应**（你嫌烦了，还是照做了）

**这四个判断全部不需要模型，纯代码就能做，而且必须纯代码做。** 理由和 5.2 一样：确定性的最可靠。让模型判断"我现在该不该打断你"，你将永远无法解释它为什么在凌晨三点开口。

### 6.2 结构：提案 → 闸门 → 播报 → 反馈

```
事件 ──→ 场景规则 ──→ nudge 提案 ──→ 闸门(纯代码) ──┬─→ 播报 ──→ 记录 outcome
                       (写入待发队列)               └─→ 丢弃(记录原因)
```

关键在于**任何主动发言都不许直接播出去，必须先变成一条提案入库**。这样才有"为什么它没提醒我"和"为什么它提醒了我"两个方向都能查的日志。

```sql
CREATE TABLE nudges (
  id INTEGER PRIMARY KEY,
  kind TEXT,            -- wakeup_brief / sit_too_long / meal_suggest ...
  urgency INTEGER,      -- 0 可有可无 / 1 普通 / 2 重要
  payload TEXT,         -- 要说的内容或生成它所需的参数
  created_at TIMESTAMP,
  earliest_ts TIMESTAMP,-- 早于此时间不发
  expires_ts  TIMESTAMP,-- 晚于此时间作废，不补播
  fired_at TIMESTAMP,   -- 空 = 还没发
  dropped_reason TEXT,  -- quiet_hours / cooldown / daily_budget / expired
  outcome TEXT          -- ignored / acked / acted / muted
);
```

### 6.3 闸门的四条规则

| 规则 | 说明 |
|---|---|
| **静默时段** | 一张时间表，硬编码即可。urgency=2 可穿透，但起步阶段建议连穿透都不给 |
| **同类冷却** | 每个 kind 一个冷却时长。`sit_too_long` 提醒完至少 45 分钟内不再提 |
| **每日预算** | **主动发言一天总共不超过 N 条**（建议起步 N=3）。用完就闭嘴，不管队列里还有什么 |
| **过期作废** | 到点没发出去就丢弃并记 `expired`，绝不补播。迟到的提醒比不提醒更烦 |

每日预算这条是四条里最反直觉也最重要的：它强制你面对"如果今天只能说三句话，说哪三句"。没有这个上限，每加一个场景就多一批提醒，系统会平滑地滑向讨厌，而且没有任何一天你会察觉到它变糟了。

### 6.4 反馈：唯一能让主动性变好的信号

播报之后必须回写 `outcome`。这是全系统唯一一个"它做得对不对"的真实标签。

- **acted / acked** — 你照做了或应了一声
- **ignored** — 默认值，没反应
- **muted** — 你明确说了"别再说这个了"

> **硬规则：`muted` 必须有确定性后果。** 该 kind 的冷却时长翻倍，或直接停用，由代码执行并落库。
>
> **绝不能是"模型记住了你不喜欢"。** 你说了"别再提醒我久坐"，结果第二天它又提醒了——这一次失信造成的损失，比这个功能全部的价值还大。

跑一个月后 `SELECT kind, outcome, count(*) FROM nudges GROUP BY 1,2` 会直接告诉你哪些场景该砍。这比任何主观感受都准。

### 6.5 起步时的保守默认

第一个主动场景上线时，建议：

- 每日预算 3 条，静默时段 22:00–07:00 无穿透
- 所有 kind 默认冷却 60 分钟
- **先跑一周"只入库不播报"**（dry run）。你自己每天翻一次 `nudges` 表，看它*本来会说什么*。这一周不花任何硬件成本，却能筛掉大部分坏点子

---

## 7. 数据获取

### 7.1 优先级阶梯

1. **官方 API** — 高德/百度地图开放平台有 POI 搜索、周边搜索，个人免费额度绰绰有余，自带评分和营业时间。"附近吃什么"90% 用它就够
2. **小程序 / 移动端接口** — 反爬弱得多
3. **带登录态的浏览器自动化** — ego-lite 或 Playwright persistent context
4. **DOM 爬取** — 最后手段

### 7.2 两个具体的坑

- **大众点评**：数字用私有字体渲染，评分和评论数爬下来是乱码，要逆向字体映射表且字体会轮换。出了名的难缠，不建议投入
- **小红书**：需要签名头和设备指纹，高频操作会封号。一天几次的低频访问基本安全

### 7.3 ego-lite 的定位与限制

核心卖点是把已登录的浏览器状态共享给 agent，agent 在独立 Space 操作不干扰你自己的标签页——对必须登录的站点，最难的那环被解决了。自带 ego-browser skill，直接对接 Claude Code / Cursor / Codex。

> ⚠️ **目前只支持 macOS**，Windows / Linux 在路线图上。与"独立跑在树莓派"对不上。现实解法：视觉与常驻逻辑在树莓派，浏览器任务丢给家里的 Mac，树莓派通过 HTTP 调它。

浏览器类任务一次十几秒到半分钟，**热路径尽量走 API 脚本**，浏览器留给"帮我查一下"这种愿意等的请求。

### 7.4 必须做的设计：降级而不是编

DOM 一改版 skill 就废。每个数据源都要有明确失败路径，**抓不到就说"我没查到"**。一个幻觉出来的不存在的餐厅，比不回答糟糕得多，而且会直接摧毁你对整个助手的信任。

### 7.5 更根本的判断

"今天吃什么"的瓶颈不是商家数据。全城餐厅列表谁都能拿到，真正缺的是：你上周吃了三次辣、你不吃香菜、上次那家你说难吃、这个月外卖超预算了。

**这些数据只有你有，才是能赢过美团推荐的唯一原因。** 高德 POI 提供候选集 + 你的饮食日志做筛选，一周就能做出来，效果比爬遍全网点评更好。

---

## 8. Skill 化与跨工具复用

### 8.1 Skill 就是一个文件夹

一个 SKILL.md（指令集）+ 可选脚本、模板、参考文档。`~/.claude/skills/` 是个人库，`.claude/skills/` 是项目级。唯一必需的文件是 SKILL.md。遵循 Agent Skills 开放标准，跨多个 AI 工具通用。

### 8.2 关键：能力写成 CLI 脚本，SKILL.md 只当说明书

```
~/.claude/skills/food-search/
  SKILL.md              # 什么时候用、怎么调、失败怎么办
  scripts/poi.py        # 高德 POI 搜索，参数化 CLI
  scripts/log_meal.py   # 记录饮食
  data/meals.jsonl
```

这样 **pi 能零改写复用**——pi 没有 MCP 但有 bash 工具，`python poi.py --near "xxx"` 这种 CLI 接口是所有 harness 的最大公约数。CC、Codex、pi、自己的事件循环，调的都是同一个脚本。

SKILL.md 务必写清四件事：何时用、参数怎么传、返回什么结构、**查不到时说"没查到"而不是猜**。

### 8.3 战略建议：先用 CC 当界面做原型

把整个助手先做成一组 CC skill，用敲字对话。不用摄像头、树莓派、语音。

跑两周会发现：**真正会反复用的场景，可能只有设想的四五个里的一两个。** 那时再决定往哪投硬件，比凭想象铺开省几个月。

---

## 9. 场景可行性评估

| 场景 | 可行性 | 说明 |
|---|---|---|
| 穿搭点评 | ★★★★★ | 主动站过去问，无需常驻检测。**视觉场景里先做这个**（整体第一个仍是饮食记录，见 13 节） |
| 起床播报 | ★★★★☆ | 离床检测很稳，一天一次，误报成本低 |
| 今天穿什么 | ★★★★☆ | 天气 API + 衣柜清单，纯文本任务 |
| 今天吃什么 | ★★★★☆ | 高德 POI + 饮食日志 |
| 姿态判断"是否认真学习" | ★★☆☆☆ | 见下 |

### 关于"是否认真学习"的警告

姿态与专注度之间**没有可靠映射**。低头看书和低头刷手机骨架几乎一样，坐姿笔挺也可能在走神。结果会是一个频繁误判、还带评判语气的东西，两天内就被关掉。

**替代做法：别判断"认真"，只播报可观测的物理事实。** 离座超过 25 分钟、连续驼背 10 分钟、手机在手中超过 5 分钟——这些是确定的，且不需要 VLM，本地骨架就够。报事实不评价人，容忍度会高得多。

---

## 10. 网络与部署

### 10.1 最被低估的风险不是"能不能连上"，是延迟

为拟人感做的所有努力——端到端模型、AEC、打断处理——会被一条 300ms 的跨境链路一次抹掉。对话感对 RTT 极度敏感，这条上没救。

**策略：按路径分开处理，而不是给整台机器配统一出口。**

| 路径 | 出口 | 理由 |
|---|---|---|
| 语音对话 | 国内（豆包） | 延迟决定体验，且核心链路不能是单点故障 |
| VLM / 慢任务 / 脚本 | 可走境外 | 多 300ms 完全无感 |

> ⚠️ 不要把核心对话链路建在跨境上——链路和账号都不受你控制。

### 10.2 树莓派网络配置要点

- **走有线，别用 WiFi**。音频流对抖动比对带宽敏感得多，偶发丢包直接变成对话卡顿。这条比任何优化都有效
- **出口在网关层解决，不在设备层**。设 `HTTP_PROXY` 环境变量对 WebSocket 经常无效（很多库不读它），会 debug 到崩溃。要么路由器层透明处理，要么设备上走 TUN
- **DNS 用 DoH**，避免解析层问题被误判成 API 故障
- **装 Tailscale**。给树莓派固定私有地址，在外面也能 SSH 回去看日志调参数
- 局域网内配静态 IP 或 mDNS（`pi.local`）

### 10.3 动手前先做的诊断

写个小脚本每分钟 ping 一次每个候选 API 端点，记 RTT 和丢包，跑一周。

**在写代码之前就知道哪条路走得通。** 否则会花两周实现语音链路，然后发现体验差的原因和代码无关。

---

## 11. 硬件方案

### 11.1 主机三条路线

| 方案 | 成本 | 适用 |
|---|---|---|
| ESP32 + 云端服务器 | ~100 元 | 最便宜，但必须联网、依赖服务器 |
| **树莓派 5 / Orange Pi 5 Plus** | ~600-900 元 | **推荐起步**，跑完整 Linux |
| Jetson Orin Nano | 高一个量级 | 只在需要本地推理小模型时 |

**单片机（Arduino / STM32 / ESP32）不能当大脑，但很适合当身体。** 几百 KB 内存、无操作系统，跑不了 agent 框架。但本项目调云端 API，需要本地算力的只有录音、唤醒词、播放、联网——ESP32 完全够。

参考：开源项目「小智 AI」（xiaozhi-esp32）就是 ESP32-S3 做前端、AI 全在云端，硬件几十块。

### 11.2 拾音方案（按折腾程度排序）

1. **USB 会议音箱**（Anker PowerConf、Jabra Speak）— 硬件 AEC，标准 USB 音频设备，插上就能 `arecord` 录到干净音。**零折腾，二三百块，推荐**
2. **小智 AI（ESP32-S3）** — 为此场景设计的开源方案，社区活跃
3. **ReSpeaker 麦克风阵列** — 需自己调，可控性最好
4. **刷小爱音箱** — 当玩具或第二房间终端

### 11.3 回声消除（AEC）是最大的硬件坑

喇叭和麦克风在同一个盒子里，没有 AEC 模型会听见自己的声音然后自己打断自己，对话直接崩。这不是软件配置问题，需要带硬件 AEC 的设备，或系统层（PipeWire + WebRTC AEC）做掉。

**预计这会是硬件上花时间最多的一件事。**

### 11.4 关于刷智能音箱

官方闭源，但老型号社区已破解：

- **xiaoai-patch** — 支持 LX06、LX01、LX05、L09A，能装 MPD / Snapcast / Shairport-Sync、Porcupine 唤醒词、Whisper，可接 Home Assistant
- **open-lx01** — 小爱音箱 mini 定制固件
- **openxiaoai** — 在小爱音箱上跑小智语音助手，且不影响原有小爱能力

方式：买三五块的 USB-TTL 刷机线，拆机焊 TX/RX/GND，PuTTY 串口 115200 连入。老固件可能直接回车 root 无密码；升级过的需靠双系统设计，重启时按键进 uboot 切分区。**部分新型号加密分区，有变砖风险。新型号基本没戏。**

> ⚠️ **关键冷水**：刷成功后得到的是"一个能跑 Linux 的麦克风加喇叭"，**厂商的远场拾音和 AEC 算法拿不到**——那套在 DSP 固件里不对外暴露。换成 Porcupine 后唤醒效果会明显不如原厂。
>
> 也就是说，想从音箱买到的那个优势，恰恰是刷机拿不走的部分。剩下的外壳、扬声器和弱 SoC，几十块钱能买到更好的。

---

## 12. 运行约束：成本、可靠性、影像隐私

> 这三件事都不是功能，但每一件都能单独杀死项目：账单让你舍不得开，故障让你不敢依赖，影像问题让你两年后不敢再让它跑。

### 12.1 成本模型：先定预算，再写代码

文中多处出现"一天几毛钱""便宜几十倍"，但那只对 VLM 和文本模型成立。**realtime 语音是唯一一个成本量级不同的路径，而且它按时长计费，包括你不说话的那段时间**（见 2.4：别让 session 挂着等慢任务）。

动手前先填一张表，数字从各家定价页现查：

| 路径 | 计费方式 | 单次估价 | 每日次数上限 | 每日成本上限 |
|---|---|---|---|---|
| realtime 对话 | 音频 token / 时长 | | | |
| VLM 场景判断 | 图片 + 文本 token | | | |
| 主动播报（文本 + TTS） | 文本 token + 字符 | | | |
| 慢任务 / 脚本 | 文本 token | | | |

**每条路径都要有硬上限和熔断降级**，写进代码而不是写进心里：

- realtime 单次 session 超过 N 分钟自动收尾
- 当日预算耗尽后，主动播报降级为静默入库（明天再说），对话降级为文本模型 + TTS
- 每次调用把 token 数和估算金额写进 `events` 表——**没有账本就没有优化**

熔断的意义不在省钱，在于**让"忘了关"这件事不再有灾难性后果**。个人项目里烧钱的从来不是正常使用，是某个循环卡住了没人发现。

### 12.2 可靠性：故障在交界处，所以契约要在交界处

14.2 已经指出故障总出现在模块交界处。那么每一个交界处都需要显式契约，起步阶段只要三样：

**① 超时与降级，写在调用方**

每个外部依赖（摄像头、模型 API、高德、Mac 上的浏览器）都要回答同一个问题：**超时多久、失败了说什么。** 默认答案是 7.4 那条——说"我没查到"，而不是编。

**② 看门狗**

常驻进程会挂，树莓派会重启。systemd 的 `Restart=always` 解决一半；另一半是**心跳**：进程每分钟往 `events` 写一条 heartbeat，一个简单脚本发现十分钟没心跳就通知你。没有这个，它会在某个周二悄悄死掉，你两周后才发现。

**③ 启动时的状态恢复**

重启后，摄像头状态机的"你在床上"是什么？未发的 nudge 怎么办？给一条简单规则即可：**启动时状态一律置为 unknown，队列里过期的 nudge 直接作废。** 不要试图恢复现场——那是分布式系统的问题，不该出现在这个规模的项目里。

### 12.3 影像与隐私：不是道德条款，是可持续性条件

5.7 讲的"收窄接口"其实是 debug 问题，不是隐私问题。真正的隐私问题在视觉链路：三层触发的第一二层在本地（Frigate + 骨架），**但第三层是要把画面帧发到云端的**。

必须明确的四件事：

| 项 | 要求 |
|---|---|
| **物理开关** | 摄像头要有能一秒关掉的方式（智能插座 / 物理挡片 / 一条 `assistant camera off` 命令）。心理上"随时能关"比任何策略都重要 |
| **上传留痕** | 每一次上云都往 `events` 写一条：时间、哪个摄像头、几帧、发给谁。**能查"它今天看了我几次"** |
| **本地保留期** | 原始帧和录像设定自动清理（如 7 天）。默认无限保留 = 几个月后你会开始介意 |
| **他人在场** | 摄像头只放自己的房间；家人或客人可能出现的区域不放。这条一旦破例，后面所有讨论都白做 |

> 补充一条现实提醒：云端厂商对上传内容通常有自己的留存策略（用于滥用审查等），这不由你控制。所以**判断标准不是"厂商会不会看"，而是"这一帧如果被别人看到，我能不能接受"**。摄像头位置按这个标准选，比任何加密都有效。

---

## 13. 实施路线图

### v0：一个周末

```
Python + SQLite + 一个文本模型 API + 一个 CLI 入口 + systemd
```

没有语音、没有摄像头、没有 pi、没有 agent 框架。先跑通 **敲字 → 查库 → 调模型 → 写库** 这个闭环。

同时：手写一个 `memories` 表，手动填十条偏好，每次对话注入 system prompt。跑一周就能感觉到"它记得我"——这一步不需要任何抽取逻辑。

### 后续顺序

| 阶段 | 内容 | 为什么这个顺序 | 两周后的通过判据（达不到就砍，不修） |
|---|---|---|---|
| 1 | 饮食记录 + 高德 POI 推荐 | 验证记忆层价值，无需任何感知硬件 | 我主动用了 ≥10 次，且 ≥3 次真按它说的吃了 |
| 2 | 穿搭点评 | 零主动，跑通视觉链路 | 连续 10 次点评里我觉得有用的 ≥6 次 |
| 3 | Realtime 语音 | 用 Pipecat，先解决 AEC | 能连续对话 3 轮不出现自打断、不出现"它以为说过我没听见" |
| 4 | 起床播报 | 第一个主动场景，低频、误报成本低 | 连续 7 天 0 误报（该播没播 / 不该播播了都算） |
| 5 | 久坐/离座提醒 | 高频，骚扰风险最大，最后做 | 一周内 `nudges.outcome = muted` 为 0 |
| 6 | 浏览器任务（ego-lite） | 明确知道缺哪块信息之后再接 | 能说出 ≥2 个"没有它就真拿不到"的具体信息 |

判据必须**事先写好、可证伪**。不然到时候你会替它找借口，然后带着一个自己其实不用的功能继续往前建。8.3 已经预言了这件事：设想的四五个场景里真正会反复用的可能只有一两个——判据就是让你认出是哪一两个。

**每加一样都要能独立回滚**，因为其中至少一半你会在两周后发现自己不用。

### 总量上限

14.2 说项目死于子系统数量。那就需要一个显式上限，而不只是"加东西的顺序"：

> **同时在跑的子系统不超过五个。要加第六个，先删一个。**

"在跑"指需要你维护、会半夜报错、坏了你得去修的东西：一个 systemd 服务、一个 Docker 容器、另一台机器、另一门语言，各算一个。CLI 脚本不算。

这条比路线图更重要。路线图管的是顺序，上限管的是终态——没有上限，正确的顺序最后也会通向一个十个模块的、大部分时间不工作的系统。

### 核心原则

**尽快让它变成你每天真在用的东西**，哪怕只是一个 CLI + SQLite 的饮食记录器。有了一个真实用户（你自己），后面每一步该做什么会自己浮出来；没有的话会一直在建，永远不上线。

---

## 14. 风险与门槛

### 14.1 两种门槛，性质不同

**学一下就会的**：SQLite、Python 脚本、API 调用、systemd、高德 POI、Frigate 部署。文档齐全、答案唯一，有 CC 帮写样板代码。一个周末的量。

**学不会的，只能磨**：

| 项 | 为什么没有教程 |
|---|---|
| **音频** | AEC、麦克风摆位、增益、缓冲大小——没有教程覆盖你的房间。只能挪喇叭、试参数、靠耳朵判断 |
| **实时链路时序 bug** | 打断对齐、二进制帧、状态不同步。不稳定复现，日志里看不出来 |
| **视觉状态机** | 你家的光线、家具、你的坐姿习惯是独一份的。参数只能拿自己的录像反复回放调出来，没人的经验能直接搬 |
| **什么时候该说话** | 完全不是技术问题。只能自己用两周，然后发现"这条提醒太烦了"。第 6 节给了它一个结构，但阈值只能靠数据慢慢磨 |

### 14.2 真正让项目死掉的原因

不是难度，是**子系统数量**。八个模块各有 5% 出错率，合起来就是一个大部分时间不工作的系统，而且故障总出现在**模块交界处**——摄像头没问题、模型没问题、数据库没问题，但连起来就是不对。这才是成本所在。

### 14.3 主动性的误报成本

**真正决定这东西活不活下来的，是它烦不烦人，不是它聪不聪明。** 主动打断的误报容忍度极低，两三次之后你自己就会把它关掉。

---

## 15. 关键决策速查

| 问题 | 结论 |
|---|---|
| 单片机能当大脑吗 | 不能，但适合当身体 |
| 核心是 realtime API 吗 | 不是，交互层半年换一代。核心是记忆层 + 场景定义 + 工具脚本 |
| 记忆需要上下文压缩吗 | 不需要，这是两件目标相反的事 |
| 需要向量数据库吗 | 不需要，SQLite + SQL 更准也更可 debug |
| 该爬大众点评吗 | 不该，先用高德 API + 自己的日志 |
| 该刷智能音箱吗 | 不该（为声学的话），买 USB 会议音箱 |
| 语音走境外还是国内 | 国内。延迟决定体验，核心链路不能是单点故障 |
| 先做哪个场景 | 饮食记录 + POI 推荐，然后穿搭点评 |
| 主动性交给模型判断吗 | 不交。提案入库 + 纯代码闸门 + 每日发言上限（见第 6 节） |
| 当前时间放哪儿告诉模型 | 缀在每轮 user 消息上，不放 system prompt。缓存按前缀匹配，前面一变后面全废（5.3） |
| 模型填时间用什么格式 | 本地时间。UTC 不出数据层——让它自己算时区会错，且错了不报错（5.6） |
| 确认要不要按风险分档 | 还没定。攒够 20 次真实更正再看，现在样本量是 1（5.7 待解决） |
| 要引入 TypeScript / pi 吗 | v1 不要，纯 Python。多一门语言就多一处交界故障 |
| 摄像头帧上云怎么控制 | 物理开关 + 每次上传留痕 + 本地定期清理，且只放自己房间 |
| 怎么决定砍掉哪个功能 | 看 `nudges` 的 outcome 统计和每阶段事先写好的两周判据，不靠感觉 |
| 系统最终能有多大 | 同时在跑的子系统 ≤ 5 个，加第六个先删一个 |

---

## 附录 A：资源清单

**框架与工具**
- pi / pi-mono（TypeScript agent 框架，pi-ai 多提供商抽象）
- Pipecat / LiveKit Agents（realtime 语音管道）
- Frigate（本地视频检测）
- MediaPipe Pose、openWakeWord、Silero VAD
- Home Assistant + LLM Vision（现成的摄像头 + VLM 方案，值得先跑通）
- DB Browser for SQLite
- Tailscale

**API**
- 火山引擎豆包端到端实时语音大模型
- Gemini Live API / OpenAI Realtime API
- 高德开放平台（POI 搜索）

**硬件改造**
- xiaozhi-esp32（小智 AI）
- xiaoai-patch / open-lx01 / openxiaoai

---

## 附录 B：模型型号对比

> ⚠️ **本附录易过期。** 2.6 说交互层半年换一代，下面的型号、价格、能力随时会变。
> **最后查证：2026-08**。使用前请重新核对官方文档；正文 4.2 的选型准则不会过期，这里的具体结论会。

| 模型 | 优势 | 注意 |
|---|---|---|
| **豆包端到端实时语音**（火山引擎） | 国内直连，延迟低，合规 | 二进制协议，需自己组帧 |
| **Gemini 3.1 Flash Live** | 有视频输入，便宜，有免费额度 | affective dialog 在 2.5 有、3.1 暂无 |
| **GPT-Realtime-2.1** | GPT-5 级推理，五档推理强度，并行工具调用，preamble | 最贵 |

**GPT-Realtime-2 的 agentic 能力**：
- 五档可调推理强度（minimal / low / medium / high / xhigh），默认 low
- 并行 function calling
- **Preamble** — 工具调用前先说"我查一下这个"，避免用户以为掉线后插话导致状态混乱
- 支持打断、话题切换、错误恢复，上下文 128K

**豆包 3.0（Seeduplex）**：2026 年 6 月 18 日上线，原生全双工，精准遵循 / 抗干扰 / 动态判停，对话中可直接调用工具（"边听边说边办事"）。彼时为邀测状态，建议先用 2.0。

---

## 修订记录

| 日期 | 改动 |
|---|---|
| 2026-08-10 | 新增第 6 节（主动性策略层）、第 12 节（成本 / 可靠性 / 影像隐私）；型号对比移入附录 B 并标注时效；路线图补通过判据与子系统总量上限；3.2 明确 v1 不引入 TypeScript |
| 2026-08-11 | v0 落地后的三条实战修正：5.2 补"模型写入必须可回显可更正"（第一天就把菜名记错了）；5.7 补"不可逆权限先别给"与"确认必须是确定性代码且只有一个实现点"。代码改动见 git 历史，这里只记决策 |
| 2026-08-11 | 打通时间线：模型此前没有时钟、写不了 `ts`、也改不了 `ts`，"昨天下午吃的"只能记成此刻。5.3 补"每轮都会变的东西缀在对话末尾，不放 system prompt"（缓存按前缀匹配，system 一变整段历史全部失配，实测每轮命中从 0 变成 ~1400 token）；5.6 补"UTC 不出数据层，模型只说本地时间"（让模型算时区它会错，且错了不报错）；5.7 记下待解决问题：确认该不该按风险分档。代码改动见 git 历史 |
