# 自然语言测试集

`check.py` 证明的是**代码按写的那样运行**，证明不了**写的那样是对的**。
分类词表、note 的用法、状态只有两个值——这些是设计判断，
只有真实的说话方式能证伪。这个文件就是那些说话方式。

**规矩：句子按真人写，不迁就工具参数。**
不写"我明天下午三点有个期末考试"，写"完了，明天三点考试我一点没看"。
前者是照着 schema 造的句子，后者才是人说的话——后者难得多，
而难的那个才有意义。

**它是探针，不是防线。** 撞出来的结论要下沉成 `check.py` 的断言，
否则下次改设计还得重探一遍。

## 怎么跑

```bash
cp cadence.db ~/cadence-testset-$(date +%F).db     # 先备份，随时能还原
python3 chat.py                                    # 按顺序念，别跳
```

按顺序念很重要：**后面的条目靠前面攒下的记录才成立**，
用户担心的那类失败只在"第二次提起"时才出现。

涉及 `correct_log` 的条目要在真终端里跑——`ui.confirm` 在非 tty 下
一律拒绝（无人会话的设计），脚本直跑会卡在确认那一步，
看起来像判断错了，其实是机制挡的。

跑完看落库结果：

```bash
sqlite3 -header -column cadence.db \
  "SELECT id,category,kind,name,ts,place,status,note FROM logs ORDER BY id;"
sqlite3 cadence.db "SELECT type,count(*) FROM events GROUP BY 1;"
```

## 标注

| | 含义 |
|---|---|
| ✅ | 现有设计应该能处理，跑它是为了防回归 |
| ⚠️ | **预判会露馅**，跑它是为了确认漏洞长什么样 |
| ❓ | 不知道会怎样 |

⚠️ 那些才是重点。**一批跑完一条都没撞出来，说明测试集太软，得重写。**

---

# 第一批（2026-08-12，25 条）

时间基准：今天 08-12 周三，周四 08-13，周五 08-14，周六 08-15，
下周一 08-17，下周三 08-19。

## A 组 · `category` 的二分够不够

轴是"有没有别人"。但很多条目真正的问题是**"这到底是不是我要做的事"**。

| # | 你说 | 撞什么 | |
|---|---|---|---|
| 1 | 我妈让我明天记得去交电费 | 有别人，但不是"约"——没人跟我碰面。归 `appointment` 是错的，归 `personal` 又丢掉了"有人在等这件事" | ⚠️ |
| 2 | 我买的那个键盘明天到 | **这不是我做的事，是发生在我身上的事。** 两个值都不对 | ⚠️ |
| 3 | 房租下周一到期，别忘了 | 没有人，也不是活动，纯粹一个 deadline | ⚠️ |
| 4 | 公司下周三团建，不去不行 | 别人组织的，我没约谁，但确实要跟一堆人在一起 | ❓ |
| 5 | 我妹下个月结婚，我得回趟老家 | 一句话两件事：一件是别人的事，一件是我的行程。会拆吗？拆了 `kind` 分别填什么 | ⚠️ |
| 6 | 上次那个医生说三个月后再去复查一次 | 跨度 90 天，而且是别人给我的约束 | ❓ |

> 第 2、3 条最能说明问题：**快递到了、房租到期是事件，不是行为。**

## B 组 · note 里的东西，下次找得回来吗

`find_logs` 能按 `kind` / `category` / `status` / `name` / `place` / 时间查，
**唯独不查 `note`**。所有靠 note 兜住的细节，只能靠"把三十天全拉回来
让模型自己读"找回。库小的时候这条路成立，但从没验证过模型真的会走它。

| # | 你说 | 撞什么 | |
|---|---|---|---|
| 7 | 这周五我去宣城找我一朋友，他说租个车带我去泾县转转，还要带我吃当地的水阳三宝 | 一句话三层信息：行程、交通、吃的。哪些进 note，哪些该单独成条 | ❓ |
| 8 | *（隔几轮）* 泾县那个事定了吗 | **`name` 是"去宣城找朋友"，`place` 是"宣城"，"泾县"只在 note 里。** 它会 `find_logs(name=泾县)` 查空然后说没有，还是会把三十天拉回来自己读 | ⚠️ |
| 9 | 我朋友说带我吃什么来着 | 同上，"水阳三宝"只在 note 里，而且这是条饮食偏好线索 | ⚠️ |
| 10 | 昨天在随园餐厅吃的烤羊腿，排队排了四十分钟，但真挺值 | 攒一条带 `place` 的历史给后面用 | ✅ |
| 11 | 哪家店要排队来着 | "排队"只在 note 里 | ⚠️ |
| 12 | 我上次在随园吃的什么 | **对照组**：`place` 是能查的，这条必须过 | ✅ |

> 第 12 条是关键对照。**它过、而 8/11 不过**，才证明问题不是"检索全坏了"，
> 而是 `note` 是唯一的死角。两条都不过，说明是别的毛病。
>
> 最坏的结果不是它编，是它**把有的说成没有**——那听起来很守规矩，
> 所以比编造难发现得多。

## C 组 · 状态只有两个值够不够

| # | 你说 | 撞什么 | |
|---|---|---|---|
| 13 | 跟张三约的那顿饭黄了，他放我鸽子 | **没有 `cancelled`。** 设计时主动否掉过，理由是"三个值会退化成两个半"。这条来验证那个决定——改成 `done` 是撒谎，留着 `planned` 会一直被追问 | ⚠️ |
| 14 | 老王那个健身我去了，忘了跟你说 | 正常的 planned → done。**确认框在这儿，要真终端** | ✅ |
| 15 | 我下周三那个课，以后每周三都有 | 表里没有"重复"这个概念。记一条？记十条？还是拒绝 | ⚠️ |

## D 组 · 时间

| # | 你说 | 撞什么 | |
|---|---|---|---|
| 16 | 下周找个时间去把牙看了 | 没有具体日期。`ts` 填什么？**编一个就是把不确定写成了确定** | ❓ |
| 17 | 今晚十一点半有球赛 | 本地 23:30 换成 UTC 是明天。`check.py` 测过这个换算，没测过真实链路 | ✅ |
| 18 | 我上周三是不是吃火锅了 | 相对时间反查，得自己算出 08-05 | ❓ |

## E 组 · `memories` 和 `logs` 的边界

| # | 你说 | 撞什么 | |
|---|---|---|---|
| 19 | 医生让我少吃辛辣 | 长期约束。该 `remember`？该进某条 log 的 note？**两个都做就是同一件事存了两遍，以后改一处另一处还在** | ⚠️ |
| 20 | 我这两个月在减脂 | 明确的长期状态，应该 `remember` | ✅ |
| 21 | 晚上吃啥好 | **这一批最值钱的一条。** 前面的减脂、少吃辛辣、刚吃过的东西，有没有真的影响推荐？如果推荐跟没这些信息时一模一样，7.5 那句"去掉个人记录之后推荐几乎不变，这功能就没有存在的理由"就应验了 | ❓ |

## F 组 · 老防线不能因为改 schema 就坏掉

| # | 你说 | 撞什么 | |
|---|---|---|---|
| 22 | 中午那碗牛肉面有点咸 | 抱怨句里夹着记录，不是"我吃了X" | ✅ |
| 23 | 哦对我还啃了个苹果，还有根香蕉 | 必须两次调用，不能合并 | ✅ |
| 24 | 今天天气是真不错 | 不许乱记，也不许沿用上一轮的食物名 | ✅ |
| 25 | 周六上午体检，下午约了人吃饭，晚上还得去机场接人 | 三件事三种分类。接机算 `appointment` 吗（是为别人做的事） | ❓ |

---

## 跑之前先写下预判

跑完拿这几条对照。**一条都没撞出来 = 测试集太软。**

1. **`category` 需要第三个值，或者需要换轴。** "快递到了""房租到期"不是行为。
   候选：加 `event`（发生在我身上的事），或者把轴改成"我要不要做点什么"
2. **`note` 是检索死角。** 大概率表现为查一次 `name`、查空、如实说"没有记录"
3. **缺 `cancelled`。** 黄了的约卡在 `planned` 里被反复追问，或者被谎报成 `done`
4. **重复性事件无处安放**
5. **`memories` 和 `logs` 的边界模型自己划不清**，同一句话两边都写

## 这一批没覆盖的

- **确认框被拒之后模型怎么办**——需要人在终端上按 n
- **超长跨度**（一年前的记录、一年后的计划）
- **多轮纠错的组合**（"不对，我说的是上周"）

---

# 第一批的结果（2026-08-12 跑完）

25 条跑完，落库 19 条记录、2 条记忆、49 次模型调用。
**五条预判里错了两条，另外撞出三个没预料到的问题。**

## ✗ 撞出来的（按严重程度）

### 1. 星期换算会错，而且没人看得见 —— 没预料到，最重的一条

同一场会话里，同一种换算，**三条对四条错**：

| 你说 | 落库 | 那天实际是 | |
|---|---|---|---|
| 老王说**周五**带我去健身房 | 08-15 | 周六 | ✗ |
| **周六**上午体检 | 08-16 | 周日 | ✗ |
| 这**周五**我去宣城 | 08-14 | 周五 | ✓ |
| **下周三**团建 | 08-19 | 周三 | ✓ |

不是系统性偏移，是**不稳定地错**——那比稳定错更难查。

而且回显是 `08-15 09:00`，**不带星期**，所以你根本没有发现的机会：
你说的是"周五"，屏幕上显示的是一串数字，两者对不对得上只有你自己心算。

这跟 5.6 记的时区那件事是同一类：**让模型做日历算术，它会错，而且错了不报错。**
当时的结论是"算术收进代码"，这次只是换了个算术。

**建议**：① `_time_hint()` 顺带给出未来七天的日期→星期对照（约 30 token/轮，
缀在 user 消息末尾，不动缓存）；② `_fmt_log` 的回显带上星期，让错误当场可见。
两件都便宜，做完这条就基本封死了。

### 2. 缺 `cancelled` —— 预判命中

> 你：跟张三约的那顿饭黄了，他放我鸽子
> cadence：这条要不我帮你标成 done？还是就这么留着？
> cadence（下一轮）：**被放鸽子就标成 done**，那条取消掉。

它自己知道不对劲（先问了而不是直接改），但词表里没有正确选项，
**只能在"撒谎说做了"和"永远挂着被反复追问"之间二选一**。

设计时否掉 `cancelled` 的理由是"三个值会退化成两个半"。这条证明理由不成立：
`cancelled` 不是"过期未确认"的近义词，它是一个用户会**主动说出口**的状态。

### 3. 兜底值 `other` 形同虚设 —— 预判方向对，表现不同

预判是"category 二分不够"。实际暴露的是另一件事：**模型宁愿用一个半对的值，也不用 `other`。**

| 你说 | 落库 | 问题 |
|---|---|---|
| 我买的那个键盘明天到 | `personal` / `purchase` | 这是**事件**不是行为，我什么都不用做 |
| 房租下周一到期 | `personal` / `purchase` | 同上，而且跟"购买"没关系 |
| 公司下周三团建 | `appointment` / `travel` | 团建不是出行 |
| 我妈让我记得交电费 | `personal` / `purchase` | "有人在等这件事"丢了 |

`purchase` 成了"跟钱和东西沾边"的垃圾桶，`travel` 成了"要出门"的垃圾桶。
**25 条里 `other` 只用了一次**（球赛）。

所以"判不准就填 other"这句话在工具描述里写了，但不管用——
模型总能找到一个词面上沾边的值。这跟"高德搜『辣的』返回八家肯德基"是同一个毛病：
**它不会说"不知道"，它会给你一个最像的。**

### 4. 重复性事件无处安放 —— 预判命中

"我下周三那个课，以后每周三都有" → 记成一条 08-19 的单次事件，
"每周三都有"降级进了 `note`。下周三不会再出现。

### 5. 它问了问题，然后自己把问题答了 —— 没预料到

第 15 条它先问"你是想让我记一条每周三上课的固定安排吗？具体是什么课、几点？"，
**下一轮没等回答就自己记了一条**（`personal/study`，08-19 09:00）。

第 16 条同样：「下周找个时间去把牙看了」→ 编了 08-17 09:00，
然后才说"看牙具体哪天、几点？现在先挂在下周一的早上了"。

先斩后奏比不问更糟：你以为那是个问句，其实库里已经写进去了。

### 6. 编了一条不存在的记录 —— 没预料到

第 21 条的回复里：

> - 08-12 中午和张三吃饭（已经过了）
> - 08-13 中午和张三吃饭

库里**只有 08-13 那一条**。它把中午的牛肉面和明天的约饭揉成了两条。
一次调用里同时做检索和摘要，就会出现这种缝合。

## ✓ 预判错了的（好消息）

### `note` 不是检索死角 —— 预判 #2 错了

四条全过，而且路径比我设想的好：

```
[8]  find_logs(place=泾县)                       ← 它记录时就把泾县放进了 place
[9]  find_logs(kind=meal, category=appointment)  ← 两级当粗筛，然后自己读 note
[11] find_logs(kind=meal)                        ← 「排队」只在 note 里，找到了
[12] find_logs(kind=meal, place=随园)            ← 对照组
```

**两级分类真正的作用不是过滤，是让"把候选全读一遍"变得便宜。**
这比设计文档里写的理由更强，值得回写进 5.5。

⚠️ 但第一次跑的时候这四条是在**同一场会话里**问的，
答案还留在上下文里，模型根本不需要检索——**那一轮测了个寂寞**。
必须新开一场空会话再问，才是真的在测检索。这条写进上面的"怎么跑"里了。

### `memories` / `logs` 的边界模型自己划得清 —— 预判 #5 错了

「医生让我少吃辛辣」→ 只调了 `remember(constraint, food.limit.spicy)`，
没有同时往 logs 里塞一条。「我这两个月在减脂」→ `remember(preference, diet.goal)`。
没有重复存储。

## ✓ 本来就该过的，过了

- **7.5 的赌注成立。** 第 21 条「晚上吃啥好」，全新会话：
  > 你医生让少吃辛辣，**川湘火锅这几类直接排掉**。中午才吃了牛肉面，面食连着吃，换换口味吧。

  减脂 + 少吃辛辣 + 今天吃过什么，三样都真的进了推荐。
  去掉个人记录，这个回答会完全不同——这正是那个功能存在的理由
- **值域不重叠的防线又响了一次。** 模型把 `travel` 填进了 `category`，
  当场 `ValueError`，重填成 `personal`，库里没有脏行，`events` 里留了一条 `bad_vocab`
- **三条老防线都在**：抱怨句里夹的记录能提取（"中午那碗牛肉面有点咸"）、
  一句话两样食物分两次调用、"今天天气是真不错"一条都不记
- **不编时间**：「我上周三是不是吃火锅了」→ 自己算出 08-05，
  用 `since`/`until` 查，查不到就说查不到
- **确认框正常**：改 `status` + 改 `ts` 两个字段都列在框里（`is not None` 那个修复生效）

## 成本

49 次调用，prompt 168K token，其中 **缓存命中 160K（95%）**。
"变化的东西缀在消息末尾"那个决定，账本上是这么兑现的。

---

# 第二批（2026-08-12，人物关系 + 分类的功能性判据）

第一批留下两个悬案：`cancelled` 要不要、分类不准要不要紧。
这一批用两个场景去回答，**判据是用户定的**：

- `cancelled`：如果"做没做成"写在 `note` 里、模型读得出来，就不需要这个状态
- 分类：不管当初分得对不对，**下次能不能从它当初选的那个词条里找回来**

我补了判据的另一半：**能找回来 ≠ 不会多找出不该有的。** 分类错的代价在假阳性这一侧。

## 场景一 · 15 件事，四个人，混合成败

按顺序讲给助手听，成败信息自然夹在句子里，不专门强调。

| # | 你说 | 谁 | 成没成 |
|---|---|---|---|
| 1 | 上上周跟小明约了去看电影，他最后一刻说来不了 | 小明 | ✗ 他放的 |
| 2 | 上周三跟老王去健身房了，练完腿疼了两天 | 老王 | ✓ |
| 3 | 上周五张姐拉我开了个复盘会，开了两个多小时 | 张姐 | ✓ |
| 4 | 上周六说好跟表弟去钓鱼，我睡过头了，放了他鸽子 | 表弟 | ✗ **我放的** |
| 5 | 周一跟小明说好中午一起吃饭，我在店里等了四十分钟他没来 | 小明 | ✗ 他放的 |
| 6 | 周一晚上跟老王又去了趟健身房 | 老王 | ✓ |
| 7 | 昨天张姐让我把季度报表交了，交了 | 张姐 | ✓ |
| 8 | 上个月跟表弟一起去打了球，他赢了 | 表弟 | ✓ |
| 9 | 上周跟小明说要一起爬紫金山，后来他说加班，改天了 | 小明 | ~ 改期 |
| 10 | 明天中午张姐约了个部门午餐 | 张姐 | 计划中 |
| 11 | 周五晚上老王叫我去打羽毛球 | 老王 | 计划中 |
| 12 | 下周二表弟过生日，得去 | 表弟 | 计划中 |
| 13 | 下周三张姐要跟我过一遍下季度的计划 | 张姐 | 计划中 |
| 14 | 小明说这周六带我去看车展 | 小明 | 计划中 |
| 15 | 下个月要跟老王去趟徒步 | 老王 | 计划中 |

探针每条**新开空会话**（第一批栽过跟头：同一场会话里问，答案还在上下文里，
模型根本不需要检索）。

| | 你问 | 结果 |
|---|---|---|
| P1 | 小明这人靠谱吗 | ✗ **一个工具都没调**，直接说"小明是谁？没听说过" |
| P2 | 小明说这周六带我去看车展，我要去吗 | ✗ 查了当天有没有冲突，但**完全没提小明的前科** |
| P3 | 我跟老王一共出去过几次 | ✗ `find_logs(name=老王)` 只捞到 1 条，就说"只有这一次" |
| P4 | 上次跟表弟去钓鱼去成了吗 | ✅ "你当时**睡过头放了他鸽子**"，并追问后来去成没有 |
| P5 | 有没有谁老放我鸽子 | ✅ **这一批最好的一条**，见下 |
| P6 | 张姐让我交的那个报表交了没 | ✅ 找到了，说已交（结尾对状态和 note 有点绕） |

## 场景二 · 分类错了，代价在哪一侧

补两条（鼠标垫、椅子），加上第一批留下的键盘/房租/电费（`purchase`）、
团建（`travel`）、球赛（`other`）。

| | 你问 | 模型实际调的 | 结果 |
|---|---|---|---|
| Q1 | 我最近买了点啥 | `find_logs(kind=purchase)` | ⚠️ 椅子/鼠标垫/键盘 ✓，但**房租和电费也被列成了"购买记录"** |
| Q2 | 之前那个键盘是啥时候到的 | `kind=purchase, name=键盘` | ✅ |
| Q3 | 团建是哪天来着 | `name=团建` | ✅ 但**走的是 name，不是 kind** |
| Q4 | 那个球赛我看了没 | `name=球` | ✅ 同样走 name |
| Q5 | 我这个月有哪些出行安排 | `kind=travel, since/until` | ⚠️ **公司团建被当成出行安排列出来了** |
| Q6 | 房租啥时候交来着 | `kind=purchase, name=房租` | ✅ 但 `name=房租` 已经够了，`kind` 是冗余的 |

---

# 第二批的结论

## ✅ `cancelled` 不需要 —— 销案

用户的判据满足了。P4 和 P5 都**只靠 `note`** 判出了成败，而且分得清责任：

> **P5「有没有谁老放我鸽子」**
> `find_logs(category=appointment)` → 拉回全部约，自己读 note →
> "放鸽子的惯犯是**小明**：08-10 等了四十分钟没来、08-07 加班改期、07-25 最后一刻说来不了。
> 老王和张姐倒是挺靠谱的。"

关键在于它**正确排除了表弟钓鱼那次** —— 那次是我放的人。
"谁失的约"这个信息只存在于 note 的一句自然语言里（"我睡过头了，放了他鸽子"），
没有任何字段编码它，模型读出来了。

**加 `cancelled` 反而会更糟**：它只能表达"没做成"，表达不了"谁的锅""什么原因""改期还是取消"。
note 一句话全带了。

## ✅ 用户那半个分类判据成立，我那半个也成立

**能找回来**（Q2/Q3/Q4/Q6 全过）—— 用户是对的，分得准不准不影响精确查找。

**但不是靠 `kind` 找回来的，是靠 `name`。** 四条里三条模型用的是 `name` 子串匹配。
`kind` 唯一被单独使用的两次（Q1、Q5）恰恰就是出假阳性的两次：

- 「我最近买了点啥」→ **房租、电费被列成购买记录**
- 「我这个月有哪些出行安排」→ **公司团建被当成出行**

所以 `kind` 的收益曲线是反的：

| 场景 | `kind` 的作用 |
|---|---|
| 精确找一件事 | **多余** —— `name` 子串就够了 |
| 列举一类事 | **有害** —— 分类不准直接变成假阳性 |
| 粗筛一大批再自己读 | **有用** —— 但 P5 用的是 `category`（二值），不是 `kind` |

**`category` 挣到了它的位置（P5 就是证据），`kind` 没有。**
这跟当初两级分类的设计理由对不上——当时以为两级都在干活，实测只有粗的那级在干活。
先不动，但下次再撞到 `kind` 的问题，该考虑的是砍掉它而不是修它。

## ✗ 撞出来的新问题

### 1. 人是一等公民，但没有任何字段和检索路径 —— 最重的一条

P1 / P3 都栽在这里：

- **P1「小明这人靠谱吗」→ 一个工具都没调**，直接答"小明是谁？没听说过"。
  库里躺着 4 条提到小明的记录
- **P3「我跟老王一共出去过几次」→ `find_logs(name=老王)` 只捞到 1 条就收工**，
  报"只有这一次"。实际 4 条，因为**人名基本都在 `note` 里，`name` 列存的是"健身房""打羽毛球""徒步"**

`find_logs(name=X)` 只搜 `name` 这一列。P5 之所以成功，是因为它碰巧用了 `category` 粗筛
把所有相关记录都拉回来了——那是运气，不是设计。

**最坏的表现是 P3 那种：查到一条就当成全部，把"有四条"说成"只有一次"。**
这跟第一批预判过的"把有的说成没有"是同一个形状，只是这次真的发生了。

### 2. 陈述句被当成查询，事实直接丢掉

第 1 条「上上周跟小明约了去看电影，他最后一刻说来不了」→
模型调了 `find_logs(name=看电影)`，没查到，回了句"没查到，要我记一笔吗"，
**然后就没记**。换个说法再试还是不记。加上"记一条："前缀才成功。

触发条件是**否定结果**：句子里有"没看成""来不了"，模型就当成没有可记的事。
一句本该入库的事实静默丢失，而且屏幕上看起来它很守规矩地查了一下。

### 3. `llm.py:675` 会被非法 JSON 打崩

```
json.decoder.JSONDecodeError: Expecting ',' delimiter
  llm.py:675  args = json.loads(tc.function.arguments or "{}")
```

模型返回的工具参数不是合法 JSON，异常直接冒泡打断整场对话。
`chat.py` 外面那层 try/except 会兜住不至于退出，但这一轮的工具调用全废。

**工具参数是交界处**（文档 12.2：契约要在交界处）。这里缺一个契约。
修法跟 `_to_utc` / `_bad_vocab` 一样：接住异常，返回一句"你的参数不是合法 JSON，重填"。

### 4. 输出时又算了一遍星期，又算错了

Q5 的回复里：`8/14 周四`（实际周五）、`8/15 周五`（实际周六）、
`8/19 周二`（实际周三）。**库里的 `ts` 是对的**，错的是它复述时自己贴的星期。

这比第一批那种更隐蔽：库是干净的，只有嘴上在错。
说明"把算术收进代码"必须做到底——`_fmt_log` 的回显直接带上星期，
模型就没有自己算的动机了。

### 5. 写入时的星期换算这次基本全对

上周三→08-05 周三 ✓、上周五→08-07 周五 ✓、上周六→08-08 周六 ✓、
周一→08-10 周一 ✓、这周六→08-15 周六 ✓、下周二→08-18 周二 ✓、下周三→08-19 周三 ✓。

**所以那个 bug 是间歇性的，不是稳定坏。** 第一批 3 对 4 错，这一批 8 对 0 错。
间歇性的错比稳定的错更值得修——你没法靠"它总是错"来防它。

## 累计成本

160 次调用，prompt 494K token，**缓存命中 458K（93%）**。
